<tr>
    <td><%= group.name %></td>
    <td><%= group.location %></td>
    <td><%= group.type %></td>
    <td>
        <%= link_to 'Edit', edit_store_group_path(group) %> |
        <%= link_to 'Show', group %> |
        <%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
</tr>