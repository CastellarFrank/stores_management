<div class="page-header">
    <div class="icon">
        <span class="ico-arrow-right"></span>
    </div>
    <h1>New Product<small>System</small></h1>
</div>

<div class="row-fluid">
    <div class="span12">
        <div class="block">
            <% if @exist_group%>
                <%= link_to raw('<span class="icon-th icon-white"></span> Cancel'), products_path, :class=>'btn' %>

                <%= render 'form' %>
            <% else %>
                <i><%= 'There is not any group yet, You have to create a group first.'%></i>
            <% end %>
        </div>
    </div>
</div>
